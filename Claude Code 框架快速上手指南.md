# Claude Code 框架快速上手指南

> 复制提示词，粘贴到 Claude Code 对话框，回车执行。每条附一句话说明。
> 🔍 = 让 AI 先搜索再给建议，适合你不确定怎么选的场景
> 💬 = 直接跟 AI 说，适合你已经知道要什么的场景

---

## 一、常用功能速查

### 项目初始化

| 提示词 | 说明 |
|--------|------|
| `初始化项目` | 创建必要目录结构，检查 Git 状态，第一次使用必做 |
| `查看项目结构` | 读取 CLAUDE.md 和 CODEBASE_MAP.md，输出当前项目模块概况 |
| `生成项目结构地图` | 运行 Cartographer，在 docs/ 下生成可导航的 CODEBASE_MAP.md |

### 日常开发

| 提示词 | 说明 |
|--------|------|
| `给 [模块] 加 [功能]` | 标准开发入口：触发门禁讨论需求 → 判断复杂度 → 自动路由到对应工作流 |
| `直接改 [文件] 的 [函数]，[描述改动]` | 跳过复杂流程，进入简单模式（≤3 文件、≤200 行） |
| `复杂需求：[描述]` | 强制进入六阶段开发流程：扫描 → Plan → 审查 → Steps → 开发 → Review |
| `并行开发 [任务A] 和 [任务B]` | 创建独立 worktree，两个任务同时推进互不干扰 |
| `用 worktree 隔离开发 [功能]` | 在独立 Git worktree 中开发，改动完全不影响主分支 |

### 调试

| 提示词 | 说明 |
|--------|------|
| `bug：[错误现象]` | 触发 Debug 工作流：最小复现 → 定位根因 → 写回归测试 → 修复 |
| `测试失败：[粘贴错误]` | Debug 工作流自动分析失败用例并修复 |
| `P0 bug：[描述]` | 触发 Debug Full 模式，启用跨模块分析和 Codex 全局扫描 |

### 代码扫描与分析

| 提示词 | 说明 |
|--------|------|
| `扫描代码库，再 [后续任务]` | 结构化扫描当前代码库，生成扫描包和 scan.db，供后续查询 |
| `全量扫描 scope [src/ docs/]` | 指定范围做 M4 全量扫描，输出完整分析产物 |
| `查 [关键词] 的影响范围` | 查询 scan.db，输出影响分析、调用链和高风险点 |

### 代码审查

| 提示词 | 说明 |
|--------|------|
| `review 当前改动` | CC 审查当前 diff：逻辑正确性、安全性、向后兼容性 |
| `Codex 深度 review` | 调用 Codex 独立审查，专注边界条件、长尾情况、兼容性 |

### Git 提交

| 提示词 | 说明 |
|--------|------|
| `/commit` | 自动生成符合 Conventional Commits 规范的 commit message 并提交 |
| `/git status` | 查看未提交改动、当前分支、worktree 信息 |
| `/git save` | 创建 checkpoint 提交保存当前进度（不推送） |

### 文档生成

| 提示词 | 说明 |
|--------|------|
| `解释 [模块/文件] 怎么工作的` | 生成架构图 + 流程图 + 概念解释文档 |
| `写 [模块] 设计文档` | 生成方案对比 + 架构设计 + 接口定义文档 |
| `总结最近代码变更` | 输出变更点 + 影响传播图 + 验证清单 |
| `更新 CHANGELOG` | 基于近期 git 提交自动生成或更新 CHANGELOG |

### 技术调研

| 提示词 | 说明 |
|--------|------|
| `调研 [技术/库/方案]` | 联网搜索 + 文档分析 + 对比结论 |
| `对比 [方案A] 和 [方案B]` | 生成含四象限图和雷达图的结构化对比文档 |

### C++ 专用

| 提示词 | 说明 |
|--------|------|
| `编译项目` | 检测 CMake/MSBuild，编译 C++ 项目并输出报告 |
| `运行 gtest` | 编译 + 运行 Google Test，输出测试报告 |

---

## 二、个性化工作流定制

### 🔍 让 AI 搜索后给你建议，再决定要不要改

这类场景你不确定"最佳实践是什么"，让 AI 搜索当前行业标准后给出选项，你选一个再落地到配置里。

| 提示词 | 说明 |
|--------|------|
| `搜索 [Python/C++/Go/TypeScript] 2024 主流代码风格规范，对比几种方案，给我推荐一个适合中小型团队的，我来决定要不要用` | 不知道选哪种风格时，让 AI 调研后给选项，而不是直接改配置 |
| `搜索当前主流的 git commit 规范，对比 Conventional Commits、Gitmoji、Angular 风格的优劣，给我推荐` | 让 AI 搜索 commit 规范选项，帮你在落地前做决策 |
| `搜索 [你的技术栈] 项目常见的 CI/CD 流水线方案，对比 GitHub Actions、GitLab CI、Jenkins，结合我的项目结构给建议` | 搜索主流 CI/CD 方案后根据你的项目情况给具体建议 |
| `搜索 C++ 内存安全检查工具的最新实践，对比 ASAN、Valgrind、Sanitizers，给出适合我当前项目的组合方案` | 搜索 C++ 内存工具链现状，给出可直接落地的工具组合 |
| `搜索 [你用的框架] 的最新性能优化实践，扫描我的代码库，指出可以优化的地方` | 联网搜索最新优化手段后结合你的实际代码给具体建议 |
| `搜索 Python/C++ 项目单元测试覆盖率的行业标准，结合我的项目告诉我现在差在哪` | 搜索测试规范后对照你的项目给出量化的改进方向 |
| `搜索 [某个算法/设计模式] 的最新实现方案，扫描我的代码库，看哪些地方可以用上` | 先搜索再结合你的代码做定向推荐，而不是泛泛而谈 |
| `搜索我用的 [依赖库名] 是否有安全漏洞或更新，给出升级建议` | 让 AI 实时查询依赖安全状态，给出有依据的升级方案 |
| `搜索 [你的语言] 静态分析工具的现状，推荐适合集成到这个框架 Hook 里的工具` | 搜索静态分析工具后给出可以直接挂到 PreToolUse Hook 的方案 |
| `搜索 monorepo 和 multirepo 在 [你的项目规模] 下的取舍，结合我的目录结构给建议` | 项目结构选型前先做调研，给有依据的架构建议 |

---

### 💬 直接跟 AI 说，明确需求直接落地

这类场景你已经知道要什么，直接发给 AI 执行修改。

**Git 与提交流程**

| 提示词 | 说明 |
|--------|------|
| `改成手动 git 提交：每次提交前展示 diff，等我确认并手动输入 commit message，不自动提交` | 关闭自动 checkpoint，提交流程完全由你掌控 |
| `commit message 统一加前缀 [feat/fix/chore]，不符合格式的提交自动阻断` | 强制 commit 规范，不合格直接拦截 |
| `push 前自动跑测试，测试失败则阻断` | 在 Hook 里加测试门禁，防止把破坏性改动推上去 |
| `每次 review 通过后自动打 tag，格式为 v[日期]-[模块名]` | 给每次通过审查的改动自动打版本标签 |

**代码规范**

| 提示词 | 说明 |
|--------|------|
| `把代码风格规范改成：[粘贴你的团队规范]` | 替换 .claude/rules/code-style.md，适配团队自己的编码风格 |
| `加规则：已有代码保持原风格不动，新规范只对新增文件和新增函数生效` | 防止 AI 把整个项目翻新，老代码零打扰 |
| `加规则：函数超过 [N] 行必须拆分，变量名不允许单字母（循环变量除外）` | 在代码风格规则里加硬性约束，AI 写代码时自动遵守 |
| `所有新增代码必须有类型标注，不允许出现 any` | 在风格规则里加类型安全约束 |
| `注释必须用英文，函数注释必须包含参数说明和返回值说明` | 统一注释规范，AI 生成代码时自动带注释 |

**测试规范**

| 提示词 | 说明 |
|--------|------|
| `加规则：每个新函数必须附带至少一个单元测试，否则 review 不通过` | 把写测试变成硬性门禁，而不是可选项 |
| `测试文件命名规范改成 [test_*.py / *.test.ts / *_test.go]` | 统一测试文件命名，方便 CI 自动发现 |
| `加规则：涉及数据库/网络的测试必须用 mock，不允许真实请求` | 防止测试依赖外部环境，保证 CI 可重复运行 |

**Debug 与编译**

| 提示词 | 说明 |
|--------|------|
| `Debug 流程改成 C++ 方式：编译优先、ASAN/UBSAN 复现、gdb 回溯，不用 print 调试` | 让 Debug 工作流用 C++ 正确姿势调试 |
| `编译失败时自动提取错误摘要，只展示关键报错，过滤掉无关 warning` | 大型 C++ 项目编译错误太多时，让 AI 帮你聚焦真正的问题 |
| `加规则：编译 warning 视为 error，CI 不通过` | 强制零 warning 标准，不让技术债积累 |

**Codex 调用方式与时机**

> Codex 有两种调用路径：**MCP**（默认，低延迟，推荐日常使用）和 **Plugin**（独立沙箱，适合需要隔离执行的场景）。以下提示词控制什么时候调用、调用哪个、自动还是手动。

| 提示词 | 说明 |
|--------|------|
| `切换到 Codex-First 模式：所有代码改动都交给 Codex，CC 只做需求讨论、计划拆解、审查验收` | CC 不写任何代码，只做大脑，Codex 做双手 |
| `把 Codex 调用路径改为默认走 MCP` | 日常开发用 MCP 路径，延迟低，适合高频调用 |
| `把 Codex 调用路径改为默认走 Plugin` | 需要沙箱隔离时用 Plugin，适合跑不可信代码或实验性任务 |
| `加规则：每次代码改动完成后自动触发 Codex 走 Plugin 路径做审查，不需要我手动发起` | 让 Codex 审查变成自动挂载的后置步骤，每次改完都跑 |
| `加规则：diff 超过 [N] 行时自动调用 Codex MCP 做第二轮 review，CC 只做第一轮` | 大改动自动升级为双轮审查，小改动不打扰 |
| `加规则：涉及 [安全/算法/核心模块] 的改动，强制走 Codex Plugin 审查，不允许只有 CC 自查` | 对高风险模块强制用独立沙箱做审查，提高安全性 |
| `Debug 时加一步：在 CC 定位根因之后，自动调用 Codex MCP 头脑风暴更多可能的原因，再决定修复方向` | 防止 Debug 思路局限，让 Codex 补充 CC 可能漏掉的方向 |
| `算法设计时加一步：CC 给出方案后，自动调用 Codex MCP 提供至少 2 个替代算法方向，对比后再选` | 避免第一个想到的方案就直接上，Codex 提供横向对比 |
| `需求讨论阶段加一步：CC 复述需求后，自动调用 Codex MCP 做一轮头脑风暴，列出潜在风险和边界情况` | 在动手前让 Codex 扮演质疑者，提前暴露遗漏的细节 |
| `重构任务开始前，自动调用 Codex MCP 扫描目标模块，输出依赖关系和潜在断点，再制定计划` | 让 Codex 先做影响分析，避免重构到一半发现漏掉依赖 |
| `把单次 diff 上限从 200 行改成 [N] 行` | 调整任务拆分粒度，太严或太宽时按需修改 |
| `审查策略改为分级：≤50 行 CC 自查，51-200 行一轮，>200 行或核心逻辑两轮，安全/支付模块三轮` | 按改动量和风险等级匹配审查力度 |

**自动文档**

| 提示词 | 说明 |
|--------|------|
| `加规则：引入新函数/类/算法、或改动超过 5 个文件、或 diff 超过 100 行时，自动在 docs/changes/ 生成变更说明` | 大改动自动留档，方便团队理解引入了什么新东西 |
| `加规则：新增公开 API 必须同步更新 docs/api/ 下的对应文档` | 让 API 文档和代码保持同步，不允许只改代码不改文档 |
| `每次 Codex 执行完后，自动生成一份"做了什么"的简报写入 .claude/memory/` | 把每次 AI 操作记录下来，方便复盘和审计 |

**性能与安全**

| 提示词 | 说明 |
|--------|------|
| `加规则：新增 SQL 查询必须走 review，不允许拼接字符串构造 SQL` | 在安全规则里加 SQL 注入防护门禁 |
| `加规则：secrets 和 API key 不允许出现在代码文件里，检测到则阻断提交` | Hook 里加 secrets 泄漏检测 |
| `加规则：新增网络请求必须有超时设置，不允许无限等待` | 防止引入没有超时保护的网络调用 |
| `删掉路由表里 C++ 和调研相关的场景，我用不到` | 精简路由判断，加快每次任务的场景匹配速度 |

---

## 三、文件速查：想改什么找哪里

| 想改什么 | 文件 |
|---------|------|
| 任务门禁、场景路由 | `CLAUDE.md` |
| Codex 参数、diff 限制、审查规则、禁止操作 | `.claude/workflows/claude-workflow-constants.md` |
| 复杂开发六阶段流程 | `.claude/workflows/claude-workflow-complex.md` |
| 并行开发流程 | `.claude/workflows/claude-workflow-parallel.md` |
| Debug 流程 | `.claude/workflows/claude-workflow-debug.md` |
| 技术调研流程 | `.claude/workflows/claude-workflow-research.md` |
| 代码审查流程 | `.claude/workflows/claude-workflow-review.md` |
| Python 代码风格 | `.claude/rules/code-style.md` |
| C++ 代码风格 | `.claude/rules/code-style-cpp.md` |
| Python/C++ 安全规范 | `.claude/rules/security.md` / `security-cpp.md` |
| Python/C++ 测试规范 | `.claude/rules/testing.md` / `testing-cpp.md` |
| Hook 脚本配置 | `.claude/settings.local.json` |
| 自动提交脚本 | `.claude/scripts/auto_checkpoint_commit.py` |
| Git 安全拦截脚本 | `.claude/scripts/git_safety_check.py` |
| 代码库扫描工具 | `.claude/skills/largebase-structured-scan/` |
| 文档生成工具 | `.claude/skills/doc-gen/` |
| 跨会话记忆 | `.claude/memory/` |
| Codex 侧约束 | `AGENTS.md` |
