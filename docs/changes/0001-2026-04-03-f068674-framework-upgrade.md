# 0001 框架升级 — 方法论内化 + BMAD 文档结构 + doc-ref skill

**日期**：2026-04-03 | **类型**：feat | **影响范围**：`.claude/` 工作流配置, `docs/` 文档结构

## 新增了什么

框架级升级，将视频方法论（框架先行、三轮迭代、善后工程师、Prompt 沉淀）内化到工作流规则，引入 BMAD 风格文档目录结构。新增 doc-ref skill 替代 Context7，支持本地 SDK/API 文档库检索。

### P1 核心流程修改
- `constants.md`：新增「Codex 调用前上下文检查门禁」（4 项必检）+「Prompt 沉淀规则」
- `workflows.md`：新增「三轮迭代原则」（功能可用→质量审查→打磨）+ 框架先行第 5 条 + Changelog 双轨自动生成规则
- `plan/SKILL.md`：新增步骤 2「框架先行：接口与边界定义」+ 接口 diff 对照检查
- `review/SKILL.md`：新增「善后工程师审查清单」（边界输入/错误处理/长尾情况/向后兼容/可观测性）

### P2 新能力
- `doc-ref` skill：本地 SDK/API 文档库检索，离线可用，支持私有 SDK
- `docs/api/`：文档库索引模板

### P3 文档结构
- `docs/changes/`：Changelog（人看版）
- `docs/prd/`：PRD 索引 + goals 模板
- `docs/arch/`：ADR 模板
- `docs/index.md`：文档总导航
- `CLAUDE.md`：新增辅助路由（PRD/doc-ref/Changelog）
- `.claude/memory/prompts/` + `.claude/memory/context/`：新建目录

## 升级注意（Breaking Changes）

- `plan/SKILL.md` 执行流程步骤编号变化（原 9 步 → 11 步，中间插入了步骤 2）
- `review/SKILL.md` 审核标准从 6 条增至 7 条

## 验证方法

- 检查 `constants.md` 包含「Codex 调用前上下文检查门禁」节
- 检查 `workflows.md` 包含「三轮迭代原则」节
- 检查 `plan/SKILL.md` 步骤 2 为「框架先行」
- 检查 `review/SKILL.md` 包含「善后工程师审查清单」
- 检查 `docs/` 下有 changes/prd/arch/api 四个子目录 + README
