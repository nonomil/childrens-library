*推荐结论：直接用 Habitica 源码改**

Habitica 是最接近你需求的成品：

| 维度 | Chorecoins | Habitica |
|------|------------|----------|
| 宠物系统 | ❌ 无 | ✅ 宠物+坐骑+装备+抽卡 |
| 任务闯关 | ✅ 积分家务 | ✅ Habits/Dailies/To-Dos + 等级 |
| APK | ✅ 62MB | ✅ Google Play 直接下 |
| 源码 | ✅ GitHub | ✅ 全开源 HabitRPG/habitica |
| UI形象 | ❌ 丑 | ✅ 像素风 RPG 角色 + 宠物立绘 |
| 离线 | ✅ 全离线 | ⚠️ 需注册账号（可自建服务器） |

**Habitica 优势：**
- **4百万用户**，UI经过验证，宠物/装备/称号体系完整
- **开源源码**：`github.com/HabitRPG/habitica`（GitHub stars 应该是万级别的）
- Android APK 直接在 Google Play 搜 `Habitica` 就能装
- 宠物形象是像素 RPG 风格，比 TamaFi 的 ESP32 嵌入式 UI 漂亮一个档次

**改造路径（最省力）：**
1. 直接装 Habitica App 给孩子用（不改代码）—— 体验最完整
2. 想改 UI/去云端：clone 源码，自己搭 API server，替换宠物图片资源

**如果你坚持要"换 UI 但功能不动"：**  
Habitica 的 Android 代码在 `habitica-android/` 子模块，宠物图在 `Habitica/src/main/assets/`, 找到宠物立绘替换成自己的 sprite 即可，其他任务/积分/等级逻辑全部不用动。

要我帮你去 GitHub 扒 Habitica 的宠物资源目录，或者直接给你 Clone 链接和改造要点？


找到了，最符合需求的是这个：

---

**🏆 推荐：Chorecoins**
GitHub - likhith1542/chorecoins: A family chore reward tracker built with Flutter. Kids earn coins b

| 项目 | 详情 |
|------|------|
| **直接下载 APK** | ✅ v1.0.1 有现成 APK（62MB）|
| **可改源码** | ✅ Flutter 完整源码开源 |
| **功能** | 家务积分系统，父母设任务→孩子完成→获金币→兑换奖励 |
| **技术栈** | Flutter（Android/iOS 双平台）|
| **数据** | 全离线 JSON，无需网络 |
| **同步** | WiFi 同步 / QR 码同步，多设备可用 |
| **最新版本** | v1.0.1（2026-03-22）|

**核心机制**：
- 父母模式（PIN 保护）：管理任务、奖励、孩子账号、审批完成
- 孩子模式（开放访问）：查看任务、余额、兑换奖励
- 积分兑换：孩子赚金币 → 兑换父母设定的奖励

**你要改造的方向**（对应笔记需求）：
- 作业闯关 → 在"家务/任务"上自定义为"每日作业"
- 宠物积分 → 加入宠物形象（用 TamaFi 的 sprite）
- 电子宠物 → 接入宠物成长系统

**下载 APK**（直接安装到手机）：
https://github.com/likhith1542/chorecoins/releases/download/v1.0.1/app-release.apk

**下载源码**（本地改 Flutter）：
```bash
git clone GitHub - likhith1542/chorecoins: A family chore reward tracker built with Flutter. Kids earn coins b
cd chorecoins
flutter pub get
# 改完打包：flutter build apk --release
```

---

另一个备选是 **HOM-E-app**（Kotlin + Firebase，0星但功能类似），这个没有现成 APK 要自己编译。如果 chorecoins 的 Flutter 代码你能看懂，就够用了。