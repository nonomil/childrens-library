# program.md — 项目契约

> 本文件定义 Agent 的行为边界、评估规则和约束。
> 人类通过迭代此文件来优化"研究/开发组织"的效率。

## 可改范围

- `src/`：核心算法实现
- `tests/`：测试代码
- `scripts/`：实验脚本

## 不可改范围

- `CLAUDE.md`：路由索引
- `.claude/rules/`：工作流规则
- `program.md` 本身（需人工确认后修改）

## 评估规则

- 评估命令：`[填写评估命令，如 pytest / python -m pytest]`
- 成功标准：`[填写成功条件，如 "所有测试通过" / "F1 >= 0.90"]`
- 时间约束：`[填写单次运行超时时间]`

## 约束

- 单次改动只做一件事（原子性）
- 改动前必须有明确的假设（"做 X 预期效果 Y"）
- 不改不相关的代码

## 回滚策略

- 验证失败 → `git revert` 回滚到上次已知良好状态
- 连续 3 次失败 → 暂停，报告用户

## 迭代模式（可选：研究阶段启用）

- 模式：explore（探索）/ exploit（利用）
- 每轮记录：假设 / 改动 / 结果 / keep/discard
- 停止条件：target_reached / budget_exhausted / plateau（连续 N 轮无改善）
